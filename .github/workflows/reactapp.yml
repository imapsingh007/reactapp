name: Deploy React App to Azure Web App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: List repository contents
        run: ls -la  # List the contents of the repository to verify where `package.json` is located

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install Dependencies
        run: |
          # If package.json is in 'reactapp', navigate to that folder; otherwise, install in the root
          if [ -f package.json ]; then
            npm install
          elif [ -f reactapp/package.json ]; then
            cd reactapp
            npm install
          else
            echo "package.json not found in the repository"
            exit 1
          fi

      - name: List npm version and path
        run: |
          npm -v
          which npm

      - name: Build React App
        run: |
          # Build step, depending on where package.json is located
          if [ -f package.json ]; then
            npm run build
          elif [ -f reactapp/package.json ]; then
            cd reactapp
            npm run build
          else
            echo "package.json not found"
            exit 1
          fi

      - name: List build folder contents
        run: |
          if [ -d build ]; then
            ls -la ./build
          elif [ -d reactapp/build ]; then
            ls -la ./reactapp/build
          else
            echo "Build folder not found"
            exit 1
          fi

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: "calc1-app-webapp"
          slot-name: "production"
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: './reactapp/build'  # Adjust this if needed based on the folder structure
